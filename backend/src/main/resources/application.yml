spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nemodb
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
#  security:
#    oauth2:
#      client:
#        registration:
#          github:
#            clientId:
#            clientSecret:
server:
  port: 8081
  error:
    include-message: always
